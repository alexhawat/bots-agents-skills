# chats-search

Wave 1 — search chats/contacts by query.

## Args
- `--query` (required)
- `--mode` browser|http (default browser)

## CLI
```bash
python3 chats-search/scripts/search_chats.py --query alice
python3 chats-search/scripts/search_chats.py --query alice --mode=http
```
