# contact-info

Wave 3 — profile/about for a contact. Opaque until capture.

## Args
- `--chat` or `--jid` (one required)
- `--mode` browser|http

## CLI
```bash
python3 contact-info/scripts/info.py --chat Alice
python3 contact-info/scripts/info.py --jid 15551234567@c.us --mode=http
```
